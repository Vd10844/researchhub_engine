__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.coverage
.coverage.*

# local runtime secrets — never commit; .env.example is the tracked template
.env

# local runtime data — never commit
jobs/
evidence/
data/
*.db
*.sqlite3
server.log

# generated contract artifacts are committed, but scratch exports are not
contracts/_scratch/
