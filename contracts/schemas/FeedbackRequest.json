{
  "properties": {
    "name": {
      "type": "string",
      "title": "Name",
      "description": "Job folder name (as returned in the result)"
    },
    "key": {
      "type": "string",
      "title": "Key",
      "description": "Document/step key, e.g. 'deed', 'parcel', 'location'"
    },
    "verdict": {
      "type": "string",
      "title": "Verdict",
      "description": "'match' | 'mismatch'"
    },
    "note": {
      "type": "string",
      "title": "Note",
      "description": "Optional free-text note from the surveyor",
      "default": ""
    }
  },
  "type": "object",
  "required": [
    "name",
    "key",
    "verdict"
  ],
  "title": "FeedbackRequest"
}