fastapi>=0.115
uvicorn[standard]>=0.30
requests>=2.32
pydantic>=2.9
pillow>=11.0
# Clerk deed/plat auto-download drives the county portal in a headless browser.
# After installing, fetch the browser once:  python -m playwright install chromium
playwright>=1.40
python-multipart>=0.0.9
# Database index/analytics — SQLite in dev, Postgres in prod (psycopg2 only needed for Postgres).
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# --- test only -------------------------------------------------------------
# The QuickPlot suites need these; the app itself does not.
#   pytest tests -q      (53 API + 15 browser + 34 unit)
pytest>=8.0
httpx>=0.27          # fastapi.testclient transport
