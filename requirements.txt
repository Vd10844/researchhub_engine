# --- Core (pinned to match mapperty/quickplot-api pyproject.toml) ----------
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.10.0
pydantic-settings==2.3.4
python-multipart==0.0.9

# --- Database (pinned to match parent) ------------------------------------
sqlalchemy==2.0.46
alembic==1.13.2
psycopg2-binary==2.9.10
psycopg[binary]>=3.2.0

# --- HTTP -----------------------------------------------------------------
httpx==0.28.1
requests>=2.32

# --- Queue / worker --------------------------------------------------------
celery==5.4.0
redis==5.0.7

# --- S3 -------------------------------------------------------------------
boto3>=1.34

# --- Engine-only (parent does NOT use these) ------------------------------
pillow>=11.0                # flood map compositing
playwright>=1.40            # clerk deed/plat scraper (python -m playwright install chromium)

# --- test only ------------------------------------------------------------
pytest>=8.0
httpx>=0.27                 # fastapi.testclient transport